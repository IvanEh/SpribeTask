allprojects {
    apply plugin:'java'
    apply plugin:'war'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile 'junit:junit:4.11'
    }

    war {
        exclude '.bowerrc'
        exclude 'bower.json'
    }
}
